# Legendary Online 协作规则

## 语言和文字

- 与用户沟通时使用简体中文。
- 项目文档使用简体中文。
- 代码注释使用简体中文，除非引用第三方 API、协议字段或标准术语。
- `Java`、`Netty`、`Protocol Buffers`、`Maven`、`Unity`、`Spring Boot`、`MySQL`、`Redis`、`Kafka`、`Flink` 等技术标识保留原文。
- 类名、方法名、变量名、配置键、命令和路径保持原始技术写法，不强行翻译。

## 项目定位

- 本项目是面向 Java 游戏服务端学习和求职展示的多人 2D ARPG 垂直切片。
- 第一阶段优先完成真实可运行的客户端和服务端闭环，不追求完整 MMORPG。
- 不提前声明尚未实现、尚未测试或尚未压测的数据和功能。

## 目录和文档

- 规划类文档放在 `plan/`。
- 说明类文档放在 `docs/`。
- Markdown 文件名使用中文。
- 文档中的命令、类名、模块名和配置键保留原文。
- 修改架构、协议、运行方式或验收标准时，同步更新相关文档。

## 技术路线

- 服务端使用 `Java 21`。
- 实时网络使用 `Netty + TCP`。
- 第一版主协议使用 `Protocol Buffers`。
- 客户端主线使用 `Unity 2D + C#`。
- 第一阶段采用模块化单体，不提前拆分微服务。
- `Spring Boot` 用于登录、管理接口和外围服务，不承载每次移动和战斗请求。
- `MySQL`、`Redis`、`Kafka`、`Flink` 按阶段引入，不进入第一阶段主链路。

## 服务端权威边界

- 客户端只发送操作意图，例如移动方向、技能编号和输入序号。
- 服务端负责最终坐标、攻击命中、伤害、死亡、经验、金币和状态广播。
- 客户端不得提交最终坐标、伤害值、奖励或怪物死亡状态。
- 网络线程不直接执行复杂游戏逻辑。
- 进入场景逻辑的操作应通过命令队列，由场景 Tick 顺序处理。
- 场景内玩家、怪物和战斗状态避免被多个线程同时修改。

## 协议规则

- `.proto` 文件是协议的主要来源。
- 已发布字段编号不得复用。
- 删除字段时使用 `reserved` 保留字段编号和名称。
- 枚举保留 `*_UNSPECIFIED = 0`。
- 破坏性协议变更必须提升协议版本，并记录迁移说明。
- 每次协议变更需要同步更新生成代码、测试和 `docs/协议规划.md`。

## 实现约束

- 优先遵循项目已有结构和局部风格。
- 保持改动范围清晰，不做无关重构。
- 新增抽象必须服务于真实复杂度，不为过早设计而抽象。
- 数据结构和协议处理优先使用明确的结构化 API，不使用脆弱的字符串拼接解析。
- 代码可读性和可审查性是验收标准之一。

## 测试和验证

- 修改后执行与改动范围匹配的聚焦测试。
- 多模块工程建立后，根目录应支持 `mvn clean test`。
- 提交或交付前执行 `git diff --check`，如果当前目录不是 Git 仓库，需要明确说明。
- 未实际执行的测试不能写成已通过。
- 涉及 Unity、数据库、消息队列、压测和外部服务时，明确区分已验证、未验证和环境缺失。

## Skills 和子代理

- 当前不默认安装额外 Skill。
- 只有当重复工作稳定出现时，才考虑沉淀自定义 Skill。
- 模块级 `AGENTS.md` 等对应模块存在后再创建。
- 子代理只在任务边界清晰、可以并行推进时使用，不作为默认流程。
